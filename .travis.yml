language: generic 
sudo: required
services:
  - docker

before_install:
 - docker build -t manohargss/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true manohargss/docker-react npm run test
